return {
    {
        "windwp/nvim-autopairs", -- esse plugin, faz se você abrir parênteses, por exeplo, ele fecha automaticamente para você.
        event = "InsertEnter",
        config = true
    }
}
